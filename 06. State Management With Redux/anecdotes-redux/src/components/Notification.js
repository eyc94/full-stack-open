const Notification = () => {
  const style = {
    border: 'style',
    padding: 10,
    borderWidth: 1,
  };

  return (
    <div style={style}>
      render notification here...
    </div>
  );
};

export default Notification;
